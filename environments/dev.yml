environment: dev
release:
  name: dev-fes-alb-status

services:
  generic-service:
    image: qca-fes
    tag: first-custom-tag-build-img
    scaling:
      minCapacity: 1
      maxCapacity: 5
  worker-service:
    image: qca-engine
    tag: first-custom-tag-build-img
    scaling:
      minCapacity: 1
      maxCapacity: 5
