environment: qa
release:
  name: qa-pending

services:
  generic-service:
    image: qca-fes
    tag: replace-with-commit-sha
    scaling:
      minCapacity: 1
      maxCapacity: 5
  worker-service:
    image: qca-engine
    tag: replace-with-commit-sha
    scaling:
      minCapacity: 1
      maxCapacity: 5
